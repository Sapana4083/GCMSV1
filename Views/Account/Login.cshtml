@model GCMS.ViewModels.LoginViewModel
@{
    Layout = "~/Views/Shared/_LoginLayout.cshtml";
    ViewData["Title"] = "Login";
}

<style>
    .cardheadermargin {
        margin-top:200px;
    }
</style>
<div class="container">
    <div class="row justify-content-center align-items-center ">
        <div class="col-md-6 col-xl-4">
           
            <div class="card shadow inline cardheadermargin">
                @* <div class="card-header text-center">
                   
                 
                </div> *@
                
                <div class="card-body ">
                    <div class="text-center my-1">
                        <img src="~/images/gcms_logo.png" />
                    </div>
                    <hr />
                    <div class="text-bold text-center fw-boldest">
                        <span >Sign In</span>
                    </div>
                    <form id="loginForm"  method="post">
                        <div asp-validation-summary="ModelOnly"
                             class="text-danger mb-3">
                        </div>
                        <div class="mb-3">
                            <label>User Name</label>
                            <input asp-for="UserName"
                                   class="form-control" />
                            <span asp-validation-for="UserName"
                                  class="text-danger">
                            </span>
                        </div>

                        <div class="mb-3">
                            <label>Password</label>
                            <div class="input-group">
                                <input asp-for="Password"
                                       type="password"
                                       id="password"
                                       class="form-control" />

                                <span class="input-group-text"
                                      id="togglePassword"
                                      style="cursor:pointer;">
                                    <i class="bi bi-eye"></i>
                                </span>
                            </div>
                            <span asp-validation-for="Password"
                                  class="text-danger">
                            </span>
                        </div>

                        <button type="submit" id="btnLogin"
                                class="loginbtn btn btn-primary w-100 mt-3">
                            Login
                            <div id="loader" style="display:none;">
                                <div class="loader-content">
                                    <div class="spinner-border text-dark"></div>
                                </div>
                            </div>
                        </button>
                       
                       
                    </form>

                    @if (TempData["Error"] != null)
                    {
                        <div class="alert alert-danger my-3">
                            @TempData["Error"]
                        </div>
                    }
                </div>
            </div>

        </div>
    </div>
</div>

@if (TempData["Error"] != null)
{
    <div class="toast-container position-fixed bottom-0 end-0 p-3">

        <div id="loginToast"
             class="toast text-bg-danger"
             role="alert">

            <div class="toast-header">
                <strong class="me-auto">
                    Login Failed
                </strong>

                <button type="button"
                        class="btn-close"
                        data-bs-dismiss="toast">
                </button>
            </div>

            <div class="toast-body">
                @TempData["Error"]
            </div>

        </div>

    </div>
}

@section Scripts {
    <script>
        $(document).ready(function () {

            $("#btnLogin").click(function () {
               
                var userName = $("#UserName").val();
                var password = $("#Password").val();

                if (userName === "" || password === "") {
                    //alert("Please enter Username and Password");
                    return;
                }

                $("#loader").css("display", "flex");

                $.ajax({
                    url: '@Url.Action("Login", "Account")',
                    type: 'POST',

                    data: {
                        UserName: userName,
                        Password: password
                    },

                    success: function (response) {

                        $("#loader").hide();

                        if (response.success) {

                            window.location.href =
                                response.redirectUrl;

                        } else {

                            showToast(response.message);

                        }
                    },

                    error: function () {

                        $("#loader").hide();

                        showToast(
                            "Something went wrong.");
                    }
                });
            });

        });
    </script>
}


